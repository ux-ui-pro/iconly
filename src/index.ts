export { createIconly } from './core';

export type {
  IconlyConfig,
  IconlyError,
  IconlyErrorCode,
  IconlyInstance,
  IconRecord,
  IconStorage,
  Logger,
  Result,
  StorageStrategy,
} from './types';
